export default {
  nodeResolve: true, // Required for resolving Node.js modules
  // Optional: Add reporters, browsers, or other configurations as needed
  // For example, to run tests in a specific browser:
  // browsers: [playwright.chromium()],
  // For more options, see: https://modern-web.dev/docs/test-runner/cli-and-configuration/
};
